<%= form_with(model: hiring, local: true) do |form| %>
  <% if hiring.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hiring.errors.count, "error") %> prohibited this hiring from being saved:</h2>

      <ul>
      <% hiring.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :year %>
    <%= form.number_field :year %>
  </div>

  <div class="field">
    <%= form.label :city_id %>
    <%= form.collection_select :city_id, City.order(:name), :id, :name, include_blank: true %>
  </div>

  <div class="field">
    <%= form.label :skill_id %>
    <%= form.collection_select :skill_id, Skill.order(:name), :id, :name, include_blank: true %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
